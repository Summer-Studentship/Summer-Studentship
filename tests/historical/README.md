# Historical Test Sandbox

The `tests/Reworked Prior Model` directory is preserved as a historical
prior-model recreation sandbox. It is not the Studentship test suite and
is not added to the active CTest graph.

The accepted G0 scaffold test target is `tsunami_tests`, defined in
`tests/CMakeLists.txt` and currently backed by `tests/smoke`.
