#pragma once

#include <cstddef>

namespace tsunami::core {

using Real = double;
using Index = std::size_t;
using Time = Real;
using Length = Real;
using Velocity = Real;

} // namespace tsunami::core
