# Historical Qt Widgets Prototype

This directory preserves the pre-WBS Qt Widgets prototype for historical
evidence only.

It is not the production application shell. The accepted G0 scaffold for
new production GUI work is `apps/tsunami_gui`, which uses Qt Quick/QML.

Do not target new production work at this directory unless a later WBS
decision explicitly reactivates or migrates the prototype.
