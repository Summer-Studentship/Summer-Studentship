# Result Storage PoC Validation 2026-08-09

Status: local Lane B validation evidence; not yet merged or remotely reachable.

## Synthetic Fixture

The synthetic Regional2D fixture is generated by
`tools.results.regional2d_result.synthetic_fixture` and carries
`data_class = SYNTHETIC`.

Validation:

```text
MPLCONFIGDIR=/tmp/codex-mpl-cache \
/tmp/codex-r10-mpl-venv/bin/python -m unittest \
  tests.results.test_regional2d_result \
  tests.results.test_regional2d_xdmf \
  tests.results.test_regional2d_visualisation
```

Result: passed, 7 tests.

Coverage:

- HDF5 round trip;
- required-field validation;
- unsupported-major-version rejection;
- XDMF generation and structural validation;
- SVG parseability and figure provenance validation.

## R10 Legacy Conversion Demonstration

Source: existing C1A-R10 h400 `limited_linear` Regional2D legacy CSV output.

Converted output root:

```text
/home/helios/SimulationData/Summer-Studentship/results/r11-regional2d-storage-poc/r10-h400-limited-linear/
```

Converted files:

```text
regional2d.h5
regional2d.xdmf
conversion_validation.json
```

Summary:

- data class: `LEGACY_CONVERTED`;
- HDF5 schema: `tsunami.regional2d.result` version `1.0.0`;
- cells: 14536;
- points: 7598;
- time levels: 121;
- XDMF structural validation: passed;
- ParaView runtime: `pvpython`;
- ParaView validation: passed;
- ParaView-visible cell arrays: `h`, `qx`, `qy`.

This conversion is an integration demonstration only. It is not new simulation
evidence and does not alter the original R10 data.

## Production Writer Status

`HDF5_SCHEMA_READY`

`HDF5_RUNTIME_INTEGRATION_DEFERRED`

The C++ solver writer is not integrated in this lane. The current evidence
establishes schema, reader/writer, legacy conversion, XDMF and visualisation
paths without changing numerical state, timestep logic or legacy outputs.
