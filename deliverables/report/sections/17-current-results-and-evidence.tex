\section{Current Results and Evidence}

Accepted current evidence is limited to already executed G6 closure material:

\begin{itemize}
  \item Regional2D real-event execution and fixed accepted coupling window;
  \item exact G5/G6 0--600 s forcing-prefix equivalence with maximum difference 0;
  \item reflection benchmark for production open-ocean outlet/lateral treatment;
  \item 300 s Local3D no-defence run;
  \item 300 s Local3D simple-rigid-barrier run;
  \item runtime boundedness, finite fields, probe output and VTK final-time output;
  \item wall-function and \(y^+\) evidence;
  \item force and moment extraction for the rigid barrier case.
\end{itemize}

The current event reconstruction is not described as observationally calibrated. No final validation accuracy is claimed.
